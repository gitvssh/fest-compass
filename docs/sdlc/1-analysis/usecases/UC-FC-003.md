---
class: Current
owner: fest-compass
last_verified: 2026-09-08
id: UC-FC-003
version: v3
status: active
summary: "필요한 자료를 기획 근거로 보관한다. M1 첫 구현 범위의 자동 검증을 추가했습니다."
covers: ["FR-EVD-5","FR-EVD-6"]
screens: ["SCR-FC-001","SCR-FC-002","SCR-FC-003"]
apis: []
acs:
  - {id: AC1, text: "Given 근거를 담은 뒤 원천 값이 바뀌었을 때 When 보관 근거를 열면 Then 선택 당시 값과 조회 조건이 유지된다."}
  - {id: AC2, text: "Given 같은 원천·조건·시점의 자료가 이미 있을 때 When 다시 담으면 Then 중복을 안내하고 후보 연결을 잃지 않는다."}
  - {id: AC3, text: "Given 보관 기획안이 참조한 근거가 있을 때 When 초안에서 제외하면 Then 기존 기획안의 근거 사본은 보존된다."}
  - {id: AC4, text: "Given 비용 원문과 담당자 해석을 담을 때 When 출처를 재확인하거나 후보에 연결하면 Then 문서 단계·범위·원문 단위·확인 수준과 담당자 판단을 별도로 보존한다."}
  - {id: AC5, text: "Given 그래프의 기간 또는 표의 항목을 담았을 때 When 원천을 갱신하면 Then 당시 값·누락·집계 방식·조회 조건·출처와 선택 범위가 유지되고 새 자료는 별도 버전으로 선택한다."}
---

# UC-FC-003 · 필요한 자료를 기획 근거로 보관한다

| 항목 | 내용 |
|---|---|
| 액터 | 지자체 축제 담당자 |
| 목적 | 선택한 자료가 어떤 기획 판단에 쓰이는지 관리한다. |
| 구현 상태 | M1 첫 기능 구현·검증. 전체 기획 연결·실무 관찰은 후속 |

## 사전 조건

지도 상세·비교 결과·개인 과거 기록 중 하나를 선택했다.

## 기본 흐름

1. 기획 근거에 담기를 누르고 저장될 지역·기간·출처를 확인한다.
2. 근거 목록에서 자료를 선택해 적용할 후보와 판단 항목을 지정한다.
3. 새 자료가 있으면 기존 사본을 보면서 새 버전으로 추가한다.
4. 불필요한 근거를 초안에서 제외하고 이미 보관한 기획안의 근거는 유지한다.

## 대안·예외 흐름

같은 원천·조건·시점 중복은 기존 근거로 안내한다. 다른 버전은 별도로 보관한다. 원문 링크가 끊겨도 당시 값과 출처 정보는 남는다. 보관본에서 사용한 근거는 초안 삭제로 지워지지 않는다.

## 공개 사례를 반영한 상세 흐름

1. 출처를 재확인하거나 후보에 연결하면: 문서 단계·범위·원문 단위·확인 수준과 담당자 판단을 별도로 보존한다.

판정에 필요한 필드와 계산은 [자료 정의](../data-contract.md)를 따른다. 외부 기관의 결재·계약·정산 검토를 이 앱에서 처리한 것으로 표시하지 않는다.

## 수용 기준

- [ ] AC1: Given 근거를 담은 뒤 원천 값이 바뀌었을 때 When 보관 근거를 열면 Then 선택 당시 값과 조회 조건이 유지된다.
- [ ] AC2: Given 같은 원천·조건·시점의 자료가 이미 있을 때 When 다시 담으면 Then 중복을 안내하고 후보 연결을 잃지 않는다.
- [ ] AC3: Given 보관 기획안이 참조한 근거가 있을 때 When 초안에서 제외하면 Then 기존 기획안의 근거 사본은 보존된다.

- [ ] AC4: Given 비용 원문과 담당자 해석을 담을 때 When 출처를 재확인하거나 후보에 연결하면 Then 문서 단계·범위·원문 단위·확인 수준과 담당자 판단을 별도로 보존한다.

- [ ] AC5: Given 그래프의 기간 또는 표의 항목을 담았을 때 When 원천을 갱신하면 Then 당시 값·누락·집계 방식·조회 조건·출처와 선택 범위가 유지되고 새 자료는 별도 버전으로 선택한다.

## 사후 상태와 검증

사용자가 선택한 기록이 개인 저장 경계 안에 남고 이전 보관본은 유지된다.
검증: [TS-FC-003](../../3-testing/scenarios/TS-FC-003.md). M1 해당 범위의 자동 검증은 [20번 검증 기록](../../../validation/20-region-explorer.md)에 있다. 전체 인수·정형 실행 명세 연결은 후속이다.
