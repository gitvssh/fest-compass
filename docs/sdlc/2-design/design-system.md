---
class: Current
owner: pickDday
last_verified: 2026-09-23
version: v5
summary: "두 목적의 위계·문구·타이포·반응형과 검수 근거입니다. 새 축제의 과업 입력·문구·상태 설계와 구현 코드(통합 검증 전)의 문구 대조를 담습니다."
---

# 화면의 위계와 공통 표현

## 2026-09-23 디자인 작업의 경계

사용자의 최신 요청에 따라 이번 보완은 [기능 상세](functional-spec.md)와 [화면 목표·기본 배치](screens.md)까지다.
최종 시각 디자인은 사용자가 개선한다. 아래 기존 토큰·타이포·간격·색상은 현행 코드 또는 과거 제안이며
새 화면의 고정 디자인으로 강제하지 않는다. 자료의 의미·필수 단위/기간·행동의 결과·상태와 복구·접근성은 유지한다.
v7의 3개 상태 시안은 기존 정상 상태 v5·v6의 보완이며 검토용 상태 이름을 제품 화면 안에 노출하지 않는다.
이 변경은 앱 UI 구현이 아니다.

### 기능 상세 v7 검수 기록 · 2026-09-23

대상은 기반 `52f2975`에서 보완한 기능 상세·화면/흐름/데이터 문서와 SCR-FC-008/012/014의 v7 편집 소스·SVG다.
기준은 dev-standard `a71fd33`의 `policy/product-experience.md`와 사용자의 기능·기획 우선 요청이다.
정상 상태 v5·v6의 기존 자료 대조 증거는 재사용하고 이번에 수치나 새 공공데이터를 추가하지 않았다.

| 관점 | 판정 | 관찰·근거와 범위 |
|---|---|---|
| 기획·흐름 | 충족 · 설계 범위 | 기능 상세 §1~6과 흐름 v7을 대조. 기존 축제는 검색·회차, 새 축제는 지역으로 진입하며 세 탐색에 기록 단계 없음. 관측연월 링크와 일반 메뉴 복귀를 구분 |
| 위계·기본 배치 | 충족 · 정적 시안 | v7 3개 SVG의 1280/390px 총6프레임을 headless로 표시하고 주 담당자가 직접 확인. 008은 단일 상태에서 자원/시기 이동, 012는 목록 유지, 014는 달력·후보와 독립 일정 상태를 읽을 수 있음 |
| 기능명·문구 경계 | 충족 · 설계 범위 | 원본 문자열과 표시본 대조. 실패한 정보 이름·짧은 재시도만 있고 내부 수집/검수 규약·점수 없음. 후보의 실제 날짜·요일·4일을 유지하고 조회 실패를 0건으로 표시하지 않음 |
| 상태·복구 | 충족 · 설계 범위 | 기능 상세 §5와 UC009/010 AC11~14 대조. 필터 제외/원천 삭제, 회차 없음/개최일 미확인, 후보 없는 달력 진입 충돌3건을 수정. 실제 API 재시도·늦은 응답은 미평가 |
| 반응형 | 충족 · 와이어프레임/사본 | 새 6프레임의 내용이 프레임 밖으로 나가지 않음. 사본의 390px 시작·기능 문서·상태 화면은 전체 가로 넘침 없음. 실제 앱 확대·긴 콘텐츠 대응은 미평가 |
| 접근성 | 미평가 · 실제 조작 | 읽기 순서·단위·달력 선택의 날짜 라벨과 목록/표 대안은 정적 확인. 사본의 모바일 펼치기만 실행 검증. 제품 포커스·키보드·보조기술 동작은 앱 구현 후 검증 |
| 필요한 정보·고지 | 충족 · 변경 범위 | 지표의 대상·기간·단위와 출처 계약 유지. 기본 상태 그림에 원천 귀속이 필요한 지도 타일이나 새 외부 콘텐츠를 추가하지 않음. 기존 법적 고지의 변경은 해당 없음 |

독립 읽기 검토에서 검색 식별·다른 지역 복귀·필터 변경 선택·목록/상세 공급의 공백을 확인해 반영했고,
주 담당자가 원문·코드·표시본을 직접 대조했다. 실제 `cursor-agent` Grok 4.6 검토는 250초 내 응답을 받지 못해 종료됐다.
이를 완료된 Grok 검토로 인용하지 않고 직접 검토 결과만 사용했다. 실제 담당자 관찰은 수행하지 않았다.

Excalidraw 3개 스키마와 SVG 반복 렌더 해시3개 일치, UC009/010 전면부·본문 각14개 일치,
문서 추적 산출물51개 누락0·lint error0를 확인했다. 새 UC의 시험 시나리오 미작성 경고28개는 유지한다.
API·실행 명세·Run 미선언은 통과로 환산하지 않는다. 앱 코드는 변경하지 않아 앱 시험·배포·운영 인수는 수행하지 않았다.

## 적용 범위·근거

SCR-FC-008~014의 개발 전 설계다. [기획 기준](../../product/planning-principles.md)과
dev-standard `6e6ce25`의 `policy/product-experience.md`를 읽고 반영했다.
이번에는 새 축제 SCR-FC-011~014에 §5의 실제 문구·첫인상 검수와 §6의 과업 입력을 추가했다.
기존 축제 v5 검수는 당시 기준 `65c4179`의 이력으로 아래에 보존한다.
아래 역할별 배치·타이포 수치는 이번 구현 제안이며 현재 앱에 반영된 값으로 기록하지 않는다.
새 시안은 회색조 배치 검수용이고 최종 색·실행 동작은 구현에서 검증한다.
자료·산식은 [데이터 시각화](data-visualization.md), 화면별 순서·예외는 [화면설계](screens.md)가 정한다.

기획 의도는 사용자에게 필요한 정보의 크기·순서·그룹으로 표현한다.
내부 수집 기준·정규화·검수 절차·시안 상태는 이 문서에 두며 제품 문구·도움말·접근성 이름·고객 응답에 넣지 않는다.
데이터의 대상·기간·단위, 처리 실패와 복구 행동은 사용자가 판단하는 데 필요한 제품 정보다.

## 새 축제 설계·구현에 전달하는 과업 입력

대상은 관광 활성화를 위한 새 축제를 구상하는 지역 공무원이다. 축제 이름·소재·장소·날짜가
아직 없을 수 있으며, 개인 기획안을 작성하지 않고도 자료를 오가며 검토를 마칠 수 있어야 한다.

| 화면 | 지금 하려는 일 · 완료 상태 | 정보와 행동 순서 |
|---|---|---|
| SCR-FC-011 | 어느 지역을 살펴볼지 선택 → 선택 지역의 자원이 보임 | 목적 선택 → 시도·시군구 → `지역 살펴보기` |
| SCR-FC-012 | 구상에 쓸 자원의 성격·위치를 확인 → 필요한 상세 또는 두 자원의 사실을 읽음 | 지역 → 세 과업 이동 → 유형·범위 → 지도/목록 → 상세·선택적 함께 보기 |
| SCR-FC-013 | 월·요일별 지역 방문 차이 확인 → 같은 지표·연도의 흐름과 수치를 읽음 | 지역 → 세 과업 이동 → 실제 연도·대상·단위 → 월별/요일별 그래프 → 필요한 표·출처 |
| SCR-FC-014 | 공휴일·행사 일정을 살펴봄 → 연월 또는 골라 둔 기간의 일정을 읽음 | 지역 → 세 과업 이동 → 실제 연월·달력 → 등록 행사 → 선택적 후보 기간 비교 |

- 참고 패턴: 기존 축제 v5의 과업 이동·현재 맥락·지역 지도와 목록·출처 상세를 재사용한다.
  회차 선택·개최 음영·D0 상대일은 새 축제에 가져오지 않는다.
- devkit `knowledge/product-experience-patterns.md`의 비교 기준 사례는 같은 기준의 정보끼리 묶는
  관점을 참고했다. 과거 방문 연도와 미래 달력 연월을 분리한다. 해당 사례의 개선 효과가 측정됐다고 주장하지 않는다.
- 자료 근거: 2025 논산 일별 보관본, Foundry 자원 세 건과 실제 소개 두 건, 보관된 2026 공휴일이다.
  자원 소개 조회·요일 집계·달력 연결은 구현 코드 있음 · 통합 검증 전이고 전국 과거 분석은 후속이다.
- 검수 기준: 이름·기록 없이 세 과업을 오가는 흐름, 서로 다른 시점의 식별, 1280/390px의 읽기 순서와
  실제 고객 문구를 확인한다. 최종 색상·실제 키보드·확대·네트워크 전이는 구현 후 인수한다.

### 새 축제의 실제 고객 문구·상태

| 화면 | 문구 | 배치·상태의 의미 |
|---|---|---|
| 011 | `어느 지역의 축제를 구상하시나요?` · `시·도` · `시·군·구` · `지역 살펴보기` | 필수 조건은 자료를 조회할 지역뿐 |
| 012~014 | `지역 관광자원` · `지역 방문 흐름` · `개최 시기` · `지역 바꾸기` | 번호가 있는 작성 단계 대신 자유 이동 |
| 012 | `논산시 관광자원` · `현재 등록 정보` · `관광지` · `문화시설` | 과거 행사의 당시 시설이나 확정 행사장으로 오인하지 않는 맥락 |
| 012 | `함께 보기` · `관광자원 목록` · `출처` | 선택한 자원의 사실 비교와 탐색 복귀 |
| 012 | `지도로 보기` · `지도 중심 선택` | 전자는 모바일 표시 전환, 후자는 지도 안의 별도 조회 조건. 지도 열기로 중심을 확정하지 않음 |
| 013 | `2025년 논산시 방문 흐름` · `외지인 · 일평균 · 명/일 · 추정` | 실제 대상·관측 연도·분모가 차트와 함께 보임 |
| 013 | `월별 흐름` · `요일별 흐름` · `수치 표` · `출처·산식` | 동일 연도 집계끼리 읽고 근거로 확장 |
| 014 | `2026년 10월` · `축제·행사 조회` · `후보 기간 비교` | 실제 달력 탐색과 선택적 기간 입력 |
| 013 자료 없음 | `이 지역의 방문 흐름을 불러올 수 없어요` + `지역 바꾸기` | 요청한 영역에 짧은 상태. 공통 자원·달력 이동은 유지 |
| 013 선택 연도 없음 | `이 연도의 방문 흐름을 불러올 수 없어요` + `연도 바꾸기` | 이전 지역·다른 연도 통계로 조용히 대체하지 않음 |
| 013 불완전한 연도 | `이 연도의 요일별 흐름을 불러올 수 없어요` + `연도 바꾸기` | 유효한 월·일별 자료는 유지하고 요일 평균 자리에서만 전달 |
| 014 정상 조회 결과 없음 | `이 기간에 등록된 행사가 없어요` + 기간 변경 | 등록 자료 조회가 완료된 경우에만 표시 |

실제 시설 소개 문구와 근거는 [데이터 시각화](data-visualization.md)의 두 자원 표에 있다.
전체 문구를 도움말에 다시 설명하지 않는다. 짧은 복구 문구는 요청 실패를 알리는 기능이며 내부 규약을 고지하는 배너가 아니다.
관측 자료가 없는 지역을 선택했다고 다른 화면으로 자동 이동시키거나 처음부터 모든 분석 탭을 숨기지 않는다.
새 지역으로 바꾸면 자원 선택·지도 중심을 해제하고 사용자가 고른 연도·달력 조건은 유지한다.
상태·초점·늦은 응답 처리는 아래 공통 계약과 UC-FC-010을 따른다.

### 새 축제 구현 코드의 문구·흐름 대조 · 2026-09-23

대상은 작업 브랜치의 `apps/web/components/new-festival`과 `app/new`다. 코드와 문자열만 읽었고 화면을 실행하지 않았다.
구현 문구는 위 설계 문구와 일부 다르며 코드가 현재 정본이다.

- 011: `어느 지역의 축제를 구상하시나요?` · `시도` · `시군구` · `지역 살펴보기` · 누락 시 `시군구까지 골라 주세요.`
- 공통: `← 지역 다시 고르기` · `지역 바꾸기` → `이 지역 보기` · 메뉴 `지역 관광자원` / `지역 방문 흐름` / `개최 시기`
- 012: `{시군구} 관광자원` · `관광지`/`문화시설` · `함께 보기에 추가` · `함께 보기 n/2` · `이 자원을 기준점으로` · `지도 중심을 기준점으로` · `기준점 해제` · 좁은 화면 `목록`/`지도` · 소개 실패 `소개를 불러오지 못했어요.` + `다시 불러오기`
- 013: `{시군구} 방문 흐름` · `관측연도` + `연도 보기` · `{연도}년 월별 일평균` · `{연도}년 요일별 일평균` · 불완전 연도 `이 연도의 요일별 흐름을 불러올 수 없어요.` + `연도 바꾸기` · 자료 없음 `{연도}년 {시군구} 방문 자료가 없어요.` · `출처·산식 보기`
- 014: `{시군구} 개최 시기` · 기존 달력·후보 기간 문구 재사용

| 관점 | 판정 | 관찰·근거 |
|---|---|---|
| 기획·흐름 | 충족 · 코드 대조 | 지역 선택만으로 관광자원에 진입하고 세 메뉴를 자유롭게 오감. 이름·주제·이유·저장 단계와 쓰기 요청·브라우저 저장소 없음 |
| 기능·문구 | 충족 · 문자열 대조 | 고객 문구에 내부 절차·검수 설명 없음. 대상·단위(`명/일 · 추정`)·지난 관측값과 시설 운영 확인 필요를 제품 정보로 둠 |
| 필요한 정보 | 충족 · 문자열 대조 | 방문 출처·산식 창에 지역 전체 추정·전망 아님·0과 누락 구분, 소개 출처 창에 소개 수집 시각과 목록과 별도의 원천 수정일 |
| 위계·구성, 상태·회복, 일관성·반응형, 접근성 | 미평가 | 실행 화면·키보드·390px·늦은 응답 전이를 보지 않음. 통합 검증과 [TS-FC-010](../3-testing/scenarios/TS-FC-010.md) 실행에서 확인 |
| 담당자 사용성 · 최종 시각 디자인 | 미평가 | 실제 담당자 관찰과 최종 디자인은 사용자 후속 |

## 현행 스타일 코드에서 확인한 토큰

2026-09-22 [Tailwind 설정](../../../apps/web/tailwind.config.ts)과
[공통 CSS](../../../apps/web/app/globals.css)의 관련 값을 직접 대조했다. 전체 토큰 복제 목록이 아니다.

| 토큰 | 코드 값 | 사용 근거 |
|---|---|---|
| paper | #f6f7f4 | html/body 배경, 조건 영역 |
| ink | #10233d | 본문 기본색 |
| navy | #071a33 | 기존 주요 버튼 |
| blue | #2667e8 | 기존 선택·초점 |
| blue-soft | #dce8ff | 기존 선택 배경 |
| muted | #65738a | 보조 텍스트. 최종 배경 대비 확인 필요 |
| font-sans | Noto Sans KR, Malgun Gothic, Apple SD Gothic Neo, system-ui, sans-serif | 현재 Tailwind 글꼴 순서 |
| focus-outline | 3px solid #2667e8; offset 3px | `:focus-visible` |
| print-background | #ffffff | 기존 인쇄 배경 |

## 새 화면의 시각적 위계 · 구현 제안

| 역할 | 데스크톱 / 좁은 화면 | 굵기·행간 | 적용 |
|---|---|---|---|
| 화면 제목 | 28px / 24px | 700, 1.35 | 한 화면의 과업. 축제명은 별도 공통 맥락 |
| 섹션·차트 제목 | 20px / 18px | 700, 1.4 | `논산시 외지인 방문 추이` 등 대상 포함 |
| 본문·입력·조작 | 16px / 16px | 400~600, 1.5 | 조건·자원 이름·복구 행동 |
| 축·범례·조건 요약 | 14px / 14px | 400~600, 1.5 | 지역·연도·단위·직선거리 |
| 요약 수치 | 22px / 20px | 600, 1.4 | 필요할 때 한 행. 이름·단위·기간과 같은 그룹 |
| 수치 표 | 14px / 14px | 400~600, 1.5 | 숫자 우측 정렬, tabular-nums, 열 제목 유지 |

본문을 12px 이하로 줄여 밀도를 확보하지 않는다. 법적 지도 귀속 등 외부 컴포넌트는 기존 조건을 유지하고
주요 정보의 폰트 축소 해법으로 복제하지 않는다. 음수/양수·선택/오류를 색 하나로만 구분하지 않는다.
위 표는 신규 화면의 읽기 역할을 정하며 기존 전역 스타일을 일괄 변경하는 지시가 아니다.

| 배치 역할 | 제안 값·동작 |
|---|---|
| 콘텐츠 너비 | 최대 1200px, 가운데 정렬. 1280px 시안에서 좌우 40px |
| 모바일 바깥 여백 | 16px, 390px 시안에서 콘텐츠 358px |
| 기본 간격 | 4/8/12/16/24/32px. 같은 그룹 8~12px, 서로 다른 과업 그룹 24~32px |
| 비교 그래프 | 데스크톱 2열, 각 영역 min-width 0. 좁은 화면 1열, 동일 Y축·하루 간격 유지 |
| 관광자원 | 넓은 화면 지도와 목록 약 6:4. 좁은 화면 목록 우선·지도 전환 |
| 공통 이동 | 이름 있는 세 과업 링크, 현재 위치는 굵기+밑줄+aria-current. 좁으면 줄바꿈 |
| 조작 크기 | 주요 조작 최소 44×44px 목표. 차트 점 자체를 유일한 조작 대상으로 삼지 않음 |
| 기존 축제 요약 | 그래프 아래 비교행. 기본 두 회차·같은 지표·단위 |
| 새 축제 비교 | 자원 두 개는 같은 사실 순서, 후보 두 기간은 실제 날짜·요일. 순위·추천 점수 없음 |

큰 빈 영역이나 장식이 정보·조작을 밀어내지 않도록 한다. 차트·지도처럼 공간을 사용하는 요소는 목적 있는 면적을 갖는다.
숫자의 증가·감소에 임의의 성공/실패색을 부여하지 않는다. 이 제품은 판단 근거를 보여주며 성과를 대신 판정하지 않는다.

## 문구의 역할

| 위치 | 기본 표현 | 전달하는 의미 |
|---|---|---|
| 목적 선택 | `기존 축제 살펴보기` / `새 축제 기획하기` | 사용자의 두 상황 |
| 기존 축제 공통 이동 | `과거 방문 흐름` / `주변 관광자원` / `개최 시기 비교` | 이동 뒤 얻을 정보 |
| 방문 차트 | `논산시 외지인 방문 추이` · `명/일 · 추정` | 지역 통계의 대상·단위 |
| 회차 제목 | `2025년 · 3.27–3.30 · 목–일 4일` | 다른 날짜·요일·개최 일수 |
| 과거 월별 차트 | `2025년 월별 방문 흐름` · `일평균 · 명/일 · 추정` | 미래 예측과 구분되는 실제 관측 연도 |
| 관광자원 조건 | `논산시 전체` · `관광지` · `문화시설` | 탐색 범위와 대상 |
| 관광자원 시점 | `현재 등록 정보` · 실제 조회일 | 과거 회차의 당시 자원과 구분 |
| 지도 거리 | `선택한 위치에서 직선거리 약 …km` | 이동시간과 다른 거리 |
| 차트 보조 행동 | `수치 표` / `출처·산식` | 자세한 근거를 필요할 때 확인 |
| 후보 기간 | `후보 A` / `후보 B` · 시작일·종료일 | 우열 없는 비교 |
| 결과 없음 | `선택한 조건의 등록 결과가 없어요` + `조건 바꾸기` | 검색 범위를 바꾸어 계속 탐색 |
| 불러오기 실패 | `불러오지 못했어요` + `다시 불러오기` | 실패를 인지하고 재시도 |

제목을 설명문과 툴팁에서 반복하지 않는다. 빈 데이터 카드를 전부 만들어 제한 문구를 나열하지 않는다.
추정이라는 지표 성격을 별도 경고 카드나 `검증됨` 배지로 표현하지 않는다.
버튼 `확인`·`다음`만으로 동작을 모호하게 만들지 않고 `회차 바꾸기`, `기간 비교` 등 과업을 쓴다.
기본 조회 화면에 저장 완료를 요구하는 주 행동을 두지 않는다. 선택적 저장은 보조 행동이다.
화면별 주 행동의 개수·형태를 일률적으로 고정하지 않는다.

## 상태와 회복

| 상태 | 화면 행동 | 보조기술·초점 |
|---|---|---|
| 축제 미선택 | 목적 선택 뒤 축제명/지역 검색과 결과. 사업·전략 입력 없음 | 검색 필드 이름과 결과 수, 결과 링크 접근 |
| 방문 자료 없음 | 축제 맥락과 자원·시기 이동 유지. 요청한 방문 영역에 짧은 상태 | 자동 이동하지 않고 복구 링크로 이동 가능 |
| 부분 방문 자료 | 관측 선과 표 유지, 결측 선 단절. 불완전 기간 요약 생략 | 표의 날짜별 `값 없음`, 색만으로 구분하지 않음 |
| 조회 중 | 해당 본문 로딩 상태. 가짜 수치·진행률 없음 | aria-busy와 짧은 상태 전달, 초점 강탈 없음 |
| 성공·조건 변경 | 최신 선택에 해당하는 자료로만 표시 | 결과 갱신 알림, 조작한 필드 초점 유지 |
| 오류 | 해당 영역 짧은 오류·재시도. 다른 정상 정보·입력 보존 | 실제 실패 시 알림, 재시도 뒤 조작 위치 유지 |
| 지도 배경 실패 | 목록 탐색·지역 조건 계속 제공 | 목록 전환과 동일 자원 선택 가능 |
| 선택 상세 닫기 | 원래 자원 목록·스크롤 위치로 복귀 | 열었던 자원으로 초점 반환 |
| 조회 취소·화면 이동 | 뒤늦은 응답 무시. 브라우저 뒤로가기로 조건 복귀 | 새 화면 제목·현재 이동 위치 식별 |
| 개인 저장 선택 시 실패 | 입력·조회 유지, 재시도·기존 파일 복구 경로 | 실제 저장 실패만 알림. 조회 기능 계속 사용 |

공공 조회는 로그인이나 새로운 권한 요구를 추가하지 않는다. 지도 중심 선택은 위치 권한 요청 없이 가능해야 한다.
기간 밖 공휴일 데이터가 없거나 조회 실패한 상태를 `공휴일 없음`으로 표시하지 않는다.
응답이 안 되는 사실은 그 위치의 짧은 상태로 전달하며 내부 정책 설명으로 바꾸지 않는다.

## 접근성·반응형 구현 계약

- 읽기 순서는 축제 또는 지역 맥락→과업 이동→조건→결과→상세다. 시각 순서와 DOM 순서를 맞춘다.
- 지도 마커와 목록을 같은 자원에 연결한다. 키보드로 목록 선택·상세 열기·닫기·복귀를 제공한다.
  지도 중심은 드래그 외에 이름 있는 좌표 입력·적용으로도 고를 수 있게 한다.
- 차트는 제목·단위·기간·수치 표를 가진다. 실제 날짜 입력·표 행 선택으로 점 선택을 대체한다.
  원문·상세는 hover 없이 열 수 있어야 한다. 색과 선 모양·직접 라벨을 함께 사용한다.
- 별도 모달일 때만 초점 가두기·Escape·원위치 복귀를 적용한다. 넓은 화면의 상세열을 불필요한 모달로 만들지 않는다.
- 작은 화면은 두 회차/후보를 세로로 배치한다. 표 내부만 가로 스크롤하며 페이지 전체는 넘치지 않는다.
  구현에서 320px·200% 확대·긴 이름을 확인한다. 항목명·단위·오류를 잘라내지 않는다.
- 본문 대비 4.5:1, 큰 글자·주요 비텍스트 요소 3:1을 구현 검증 목표로 둔다.
  회색조 정지 시안은 최종 색상·키보드·보조기술 실행 검증의 통과 증거가 아니다.
- 지도 제공자 귀속과 기존 적용 고지는 유지한다. 출처·계산 상세를 접더라도 대상·기간·단위는 본문에 남는다.

## 이전 기능에 유지하는 계약

SCR-FC-001~007 구현 문서는 현행 동작을 기록한다. 새 배치가 개발됐다고 역으로 해석하지 않는다.
개인 저장의 성공/실패·충돌, 비용의 원금액·단위·세금·미산정, 인쇄의 선택 자료와 출처 보존은 유지한다.
관측·가정·실측의 구분은 필요한 제목·열·단위·그룹으로 전달하며 일률적인 상태 배지를 강제하지 않는다.
금액 막대는 0축, 파이/도넛은 실제 금액·분모·비율을 함께 제공한다.

## 새 축제 v6 설계 검수 기록

2026-09-22 주 담당자가 렌더된 SCR-FC-011~014 **4개 화면·10개 프레임**을 직접 확인했다.
1280px·390px의 지역 선택, 관광자원 목록/상세와 함께 보기, 방문 그래프, 개최 달력이 대상이다.
자료·현행 코드 기준은 main `7d6678f`, 검수 대상은 이번 v6 파일, 정책은 dev-standard `6e6ce25`다.
Grok 4.6의 자료·흐름 조사와 문서 교차 검토를 사용했고, 결과의 산식·기록 경계·연도 변경·실제 문구는 직접 대조했다.
별도 첫인상 검수에는 사용자·과업과 렌더 이미지 011~013만 전달했다. 이는 실제 담당자 관찰이 아니다.

| 관점 | 결과 | 관찰·근거·조치 |
|---|---|---|
| 기획·흐름 | 충족 · 설계 | [011](../../assets/SCR-FC-011/wireframe-v6.svg)은 시도·시군구만 입력. [012](../../assets/SCR-FC-012/wireframe-v6.svg)에서 자원·방문·시기 이동이 보이고 기록 완료 단계가 없음 |
| 위계·구성 | 충족 · 시안 | 지역 맥락→세 과업→조건→결과 순서. 012는 지도/목록·상세, [013](../../assets/SCR-FC-013/wireframe-v6.svg)은 월별/요일별, [014](../../assets/SCR-FC-014/wireframe-v6.svg)는 실제 연월 달력으로 구분 |
| 기능·문구 | 충족 · 시안·문서 | 위 실제 문구 표와 Excalidraw text를 직접 대조. 상세를 여는 자원명 밑줄과 비교 체크 열 `함께 보기`를 구분. 지도 표시 전환과 중심 지정, 기간 입력을 여는 행동은 화면 문서에 명시 |
| 상태·회복 | 충족 · 설계 계약 | 지역 변경의 선택 연도 유지, 일부 월/일 자료 유지와 요일 평균 미생성, 후보 없는 달력, 실패·늦은 응답·재시도를 UC-FC-010과 정렬. 정지 그림은 정상 조회·선택·행사 조회 전 상태만 검수 |
| 일관성·반응형 | 충족 · 1280/390 정지 시안 | 자원 비교의 모바일 한 열 배치 추가. 주소·소개가 프레임 안에서 줄바꿈됨. 013 수치 표·출처를 연도 옆으로 이동해 그래프 아래까지 스크롤하지 않아도 보임 |
| 접근성 | 충족 · 설계 계약 | 고객 프레임 글자 최소14px(011은16px). 013 축·보조글은 #555로 강화. 실제 이름·단위·선택 표시와 표/목록 대안을 설계. 최종 대비·키보드 실행은 아래 별도 범위 |
| 필요한 정보 | 충족 · 시안·자료 대조 | 013 실제 2025년·외지인·일평균·추정 단위, 014 실제 2026년 10월과 확인된 공휴일 세 건. 근거가 없는 행사 개수 대신 조회 행동. 시설 소개는 실제 두 원문에 한정 |
| 실행 상호작용·최종 색상 대비 | 해당 없음 · 문서 작업 | 신규 앱 화면 미구현. 조회/오류·키보드·보조기술·320px/200% 확대는 UC-FC-010 구현 후 시험 |
| 담당자 사용성 | 미평가 | 실제 지역 공무원의 탐색·기획 판단 효과를 관찰하지 않음 |

위 표는 2026-09-22 설계 검수 기록이다. 이후 구현 코드가 생겼으며 실행 검수는 위 2026-09-23 대조의 미평가 항목으로 남아 있다.

첫인상에서 상세 열람과 비교 선택의 차이, 모바일 수치 표 접근과 옅은 축 글자가 지적돼 위 수정 후
영향받은 012·013 표시본을 다시 확인했다. 자원 간 실제 이동 관계는 이번 사실 비교로 판단할 수 없으며
임의 이동시간을 넣지 않았다. **일반화할 수 있는 설계 관찰**은 서로 다른 선택 행동에 각각의 조작 이름을
주고, 긴 그래프 앞에 표 접근을 두는 것이다. 정지 화면의 읽기 개선이며 사용성 향상 수치를 측정한 결과가 아니다.

직접 검증: 논산 2025년 365일을 재집계해 월별 12개·요일별 7개 평균과 실제 시안 막대 38개의 높이를 대조했다.
Foundry 이름·좌표 표본과 소개 두 건, 보관 공휴일의 실제 날짜·확인 범위는 [시각화 계약](data-visualization.md)에 근거를 남겼다.
`validate.mjs`는 새 v6 4개와 함께 읽은 011 v5 1개를 통과했다. 새 SVG 4개는 제작자가 두 번 렌더한 결과와
주 담당자가 확인한 최종 파일 해시가 일치한다. `validate-mermaid.mjs ia.md flows.md`는 12개 블록 통과다.
`trace_build.py --config traceboard.yaml --no-remote`와 `trace_lint.py --no-update-trace`는 산출물 누락0,
error0·warn20이다. 경고는 UC-FC-009·010 각각 10개 AC에 연결할 TS가 아직 없다는 뜻이며 개발 검증으로 남긴다.
API·실행 기록·티켓 미선언으로 건너뛴 규칙을 통과로 기록하지 않는다. UC010 전면부·본문 AC10개는 일치한다.
이번에는 앱·운영 배포나 문서 사이트 재게시를 수행하지 않았다.

## 기존 축제 v5 설계 검수 기록

정성 검수 대상은 v5 흐름과 렌더된 네 화면의 데스크톱·390px 배치다.
구조 검사와 수치 재계산은 시각 품질·실행 상호작용 검증을 대신하지 않는다.
2026-09-22 주 담당자가 최종 SVG 표시본의 **4개 화면·9개 프레임**을 직접 확인했다.
논산 방문 36개 날짜를 데스크톱·모바일에 그린 72개 좌표를 원본 값과 대조했고,
세 회차 평균·월별 일평균·후보 날짜 요일·Foundry 관광자원 3건을 직접 재확인했다.
검토 기준은 dev-standard `65c4179`, 자료·코드 기준은 main `5bbb559`와 본 변경의 설계 파일이다.

| 관점 | 결과 | 근거·조치 |
|---|---|---|
| 기획·흐름 | 충족 · 설계 | 첫 목적 선택→축제 검색→방문 흐름, 세 과업 자유 이동. 기록·전략 입력 전이 없음 |
| 위계·구성 | 충족 · 시안 | 방문 그래프, 지도/목록/상세, 과거 월별 값과 후보 달력을 서로 다른 과업으로 배치 |
| 기능·문구 | 충족 · 시안 | 평균 단위를 명/일로 수정. 고객 프레임 내부에 내부 규약·검수 상태 없음. 후보 A/B·두 후보 뒤 기간 재선택 일치 |
| 상태·회복 | 충족 · 설계 계약 | 초기·부분자료·실패·재시도·조건 변경은 화면/UC에 명시. 시안에서 검색 결과·자원 선택·후보 일정 일부 로딩 확인 |
| 일관성·반응형 | 충족 · 1280/390 정지 시안 | 회차 세로 배치·지도 대신 목록·주소 줄바꿈 확인. 고객 프레임 글자 최소14px, 초안의 작은 축/탭 글자 확대 |
| 접근성 | 충족 · 설계 계약 | 이름·단위·수치 표·목록 대안·초점 복귀 정의. 색만으로 선택을 전달하지 않는 시안 확인 |
| 필요한 정보 | 충족 · 설계 | 지역 방문 추정·실제 연도·현재 등록 자원·출처 접근 유지. 기존 귀속/필수 고지 보존 계약 |
| 실행 상호작용·최종 색상 대비 | 해당 없음 · 이번 문서 작업 | 새 앱 화면 미구현. 키보드·보조기술·320px/200% 확대·조회/오류 실제 전이는 구현 후 UC-FC-009로 검증 |
| 담당자 사용성 | 미평가 | 실제 지역 공무원 관찰·의사결정 효과를 평가하지 않음 |

추가 수정: 결측이 개최기간 밖에만 있으면 정상 개최기간 평균을 유지하도록 범위를 명료화했다.
개최기간 평균의 누락은 회차 변경으로 복구하며 차트 기간을 줄여 분모를 바꾸지 않는다.
관광자원은 검색을 먼저 강제하는 초기 그림을 실제 지역 목록·선택 상세 그림으로 교체했다.
지표명 `월평균`은 새 계약에서 `월별 일평균`으로 정렬했다.

구조 검증: Excalidraw 스키마 4개, SVG 재렌더 바이트 일치 4개, Mermaid 10개 블록 통과.
추적 빌드의 화면 11개(기존7+신규4)와 표시본 존재를 확인했다. 린트 error 0이며 신규 UC의
TS 미작성 경고 10개는 설계 단계의 남은 개발 검증으로 유지했다. 미선언 API·실행 기록은 통과로 환산하지 않는다.
이번에는 앱·운영 배포나 내부 문서 사이트 재게시를 수행하지 않았다. 공개 API 표본 조회 1회는 HTTP403으로
본문을 받지 못해 운영 정상성을 판정하지 않았고, 관광자원 표본은 승인된 Foundry 읽기 경로로 확인했다.
