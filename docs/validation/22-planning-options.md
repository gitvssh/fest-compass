---
class: Current
owner: fest-compass
last_verified: 2026-09-08
version: v1
summary: "M3 기획 후보·근거·장소·준비를 공개했습니다. 단위 154건·신규 headless 흐름 27개가 통과했고 공개 앱의 실제 자료 연결·보관과 기존 예측 보존을 확인했습니다."
---

# 올해 기획 후보 검증

사용자의 다음 작업 요청에 따라 M3를 구현한다. 정본 요구는 FR-EVD-5/6, FR-PLN-1/2/3,
UC-FC-003/004와 SCR-FC-003/004다. 전용 worktree에서 수정하고 브라우저 검증은 headless로 수행한다.

## 구현 범위

`/planning/options`에서 담당 지역·사업·복수 수행 관계, 최대 6개 후보, 근거 연결,
같은 항목 비교·공통 날짜 일정 그래프·준비 과제 개수, 설치/행사/철거 확인과 준비 과제를 기록한다.
최대 20개 불변 보관본·파일 복원·다른 창 충돌 방어를 개인 브라우저 저장 경계에서 제공한다.
M4 예산 산출·M5 완성 기획안·공식 승인·공동 저장을 완료했다고 표시하지 않는다.

구현 설명: [기획 후보 검수 안내](../sdlc/2-design/planning-options-implementation.md).

## 검증 명령과 현재 결과

| 검사 | 명령 | 결과 |
|---|---|---|
| 신규 자료·보관·실패 반례 | `tsx --tsconfig tsconfig.test.json --test lib/planning/planning.test.ts` | 11개 통과 |
| 기존 포함 단위 | `npm test` | TypeScript 153 + SQLite 1, 합계 154개 통과 |
| 타입 | `npm run typecheck` | 통과 |
| 제품 빌드 | `npm run build` | 통과, 기존 겨울 고정 코드 14개 검사 포함 |
| 운영 의존성 | `npm audit --omit=dev --omit=optional --audit-level=high` | 취약점 0 |
| 배포 계약 | `python3 -m unittest discover -s infra/scripts -p 'test_*.py'`, `python3 infra/scripts/validate_argocd_registration.py` | 40개 통과, 17개 리소스 |
| 기존 + 신규 브라우저 | `npm run test:e2e` | 기존 편집/운영/지도/비교와 신규 기획 27개 흐름 통과 |

Node 24.20.0 / npm 11.19.0. 단위 시험의 비교 근거는 기존 검토된 논산 3회차 D0 실제 값
48,296.5 / 56,252 / 52,671.5와 원문 출처를 사용한다. 후보·담당 역할·확인 메모·장소·일정은 시험 입력이다.
자동화는 외부 기관에 연락하거나 장소 사용을 확인하지 않는다.
원주 지원사업 집행 35,118,200원·재원 합계·세금 미확인과 가상 지역 자원의 좌표 결측이 기획 사본에서도 유지됨을 확인했다.
브라우저 오류와 앱 쓰기 요청은 0건이고, 비교표 내부 스크롤을 유지하면서 390px 문서 가로 넘침은 0이다.
선택창에 명시적 접근성 이름을 추가했고 같은 자료가 두 후보에 보이는 검증 선택자를 한 후보로 한정한 뒤 전체 회귀를 다시 실행했다.
실제 로컬 화면 4개는 구현 검수 안내에 연결했다. 개인 자료의 서버 전송·기관 승인·전체 MVP 완료를 뜻하지 않는다.

## 확인할 주요 반례

- 담당 지역과 다른 지역의 근거를 연결해도 담당 지역과 자료의 공간 단위를 바꾸지 않음.
- 같은 근거/판단 중복, 원천이나 초안 수정 후 이전 보관본 보존, 파일 복원의 양쪽 이력 보존.
- 행사 구간만 확인한 장소가 설치·철거에도 사용 가능으로 표시되지 않음.
- 종료일 역전 거부, 기간 일부 중첩 허용, 기간 변경 시 이전 확인 보존·준비 재확인.
- 참조 추가만으로 완료·외부 확인되지 않음. 해당 없음의 사유/판단일, 자기·순환·타 후보 의존 거부.
- 후보 복사 시 선택·외부 확인·기한/진행 초기화. 선행 미완료 시 예외 이유 요구.
- 손상 파일·공간 부족·다른 창 변경에서 저장값과 미저장 입력 보존.

## 공개 확인

2026-09-08 [공개 앱](https://kto.damecasol.com/planning/options)에 반영했다.
소스 `590c80599c07113df19ad6900c64122fa6887afd`, 배포 선언 `c2d45fed92463d705c793082dc402916fe152fc9`다.
[CI 34208955839](https://github.com/gitvssh/fest-compass/actions/runs/34208955839)가
전용 `homelab-fest-compass` 러너에서 성공했고 원격 검증한 이미지
`sha256:00322425feb7b9d1d26237adad224bb5dcf4f33a78e092b3f06f2d080ced7846`를 사용한다.
등록 앱의 수동 반영 후 Synced/Healthy와 단일 replica를 확인했다.

공개 headless 브라우저에서 강경근대거리의 현재 관광자원과 논산 3회차의 실제 D0 자료를 담고
장소·시기 판단에 연결했다. 공주시 담당 지역을 유지한 채 검수용 두 후보를 비교·보관한 뒤
초안의 근거 연결을 해제해도 보관본이 같고 52,671.5가 다시 보임을 확인했다.
후보 이름·장소·일정은 검수용 입력이며 실제 기관의 기획·확인 결과가 아니다.
390px 문서 가로 넘침은 0이다. 일반 Python HTTP 요청은 공개 경계에서 403을 받아
통과로 간주하지 않았으며, 실제 브라우저에서는 홈·지도·비교·근거·기획·robots·sitemap·예측 3경로 모두 200이었다.
기존 예측 2개·겨울 시험·수집 식별값과 갱신 시각을 제외한 모니터 값의 전후 해시가 같았다.

내부 Traceboard 게시 대상은 이 기록·기획 후보 검수 안내·실제 화면 4개다.
게시 후 파일 일치·그림·모바일 검증 결과와 배포 증거는
`docs/validation/evidence/2026-09-08-planning-options-production.json`에 기록한다.
실무자 관찰·전체 TS 인수·정형 실행 보고서 연결은 미완료다.
