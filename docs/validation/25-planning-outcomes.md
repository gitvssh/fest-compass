---
class: Current
owner: fest-compass
last_verified: 2026-09-09
version: v1
summary: "M6 개발·단위 183개·신규 headless 14개 검증 완료. 앱 이미지 업로드는 저장소 한도로 실패해 정리 승인이 필요하며 로컬 실제 화면을 Traceboard 검수본으로 제공합니다."
---

# M6 비용·결과와 다음 회차 검증

사용자 요청으로 M6를 goal로 설정했다. [구현 범위와 사용 순서](../sdlc/2-design/outcome-implementation.md)를 따른다.
공개 집행·정산 자료와 담당자 입력을 구분하며 미확인 값을 0이나 실제 비용으로 추정하지 않는다.

## 자료 확인

공개 비용 표본은 기존 `apps/web/data/festival-editions.json`을 사용한다. 운영 모델·겨울 시험·원천 방문 이력은 바꾸지 않았다.
2026-09-09 원주시 공식 원문을 다시 조회해 HTTP 200, 122,368 bytes, SHA-256
`564013dab4ef2015b74c862d0ac1e194dcf17ff8a4bc3762796f7fb56336b105`로 기존 확인본과 일치했다.
원주 지원사업 계획/집행과 논산 일부 용역 입찰금액을 서로 다른 범위·단계로 표시한다.
공개 자료 전사 입력은 담당자 입력으로 표시하고 검증된 조회값으로 승격하지 않는다.

## 자동 검증

- Node 24.20.0, 전용 worktree에서 단위 183개(기존 175 + 신규 결과 8개) 통과.
- 신규 단위: 정의·단위·기간·출처·계획 0, 재원 합계·결측, 범위/세금/단계/중복, 불변 P/R, 차기 초기화·개선 연결, v1~v4 호환·입출력, 손상·숫자·참조·한도·충돌 방어.
- 기존 편집·개인 작업공간·지역 14·축제 비교 21·후보 27·예산 23·기획안 14 headless 흐름 통과.
- 첫 신규 브라우저 실행에서 390px 결과 표가 431px로 넘친 것을 발견해 표 내부 가로 이동으로 수정했다. 이 실패를 통과로 처리하지 않는다. 최종 신규 흐름·PDF·배포 확인을 아래에 추가한다.
- 배포 선언 테스트 40개·prod 자원 17개 통과. CI와 같은 운영 감사(`--omit=dev --omit=optional`) 취약점 0. 선택적 Prisma 개발 의존성까지 포함한 별도 조회에서는 3개 high가 보였으며 운영 감사와 구분한다.

## 인수 기준 대응

| 기준 | 검증할 결과 |
|---|---|
| AC1·AC8 | 다른 정의는 그래프/차이 보류, 같은 정의 100→120, 계획 0 비율 미산정, 표·출처 |
| AC2 | 공개 사례와 개인 과거 결과를 같은 이름·지역·연도 조건으로 분리 조회, 외부 쓰기 없음 |
| AC3·AC6 | 차기 일정·선택·실측·외부 검토 초기화, 전회차 문제/조치·홍보비 검토가 준비 과제로 연결 |
| AC4 | 가상 보조금 30,000,000·자부담 5,000,000→5,118,200, 전체 35,000,000→35,118,200, 빈 금액과 0원 |
| AC5 | 계약 참조만 있는 증빙은 미확인 유지, 정산 완료 자동 판정 없음 |
| AC7 | P1/R1 출력 후 R2 36,000,000으로 수정해도 P1/R1·35,118,200·당시 근거 유지 |

공개 원문 표본과 가상 입력 시험을 구분한다. 시험용 지원사업 금액이 실제 사례와 같아도 개인 결과로 입력한 자료는 가상이다.
실무자 관찰·전체 업무 인수·정형 Spec/Run 연결은 미실시다.

## 최종 로컬 검증

모바일 수정 후 신규 headless 14개 흐름 전체 통과, 브라우저 오류·앱 쓰기 요청 0, 390px 페이지 가로 넘침 0이다.
실제 R1 PDF는 A4 19쪽이며 35,118,200원·가상 100/120·미확인/0원·P/R ID·52,671.5명과 출처가 포함된다.
문자 추출과 컬러/흑백 페이지 렌더로 막대·표·증빙 상태·개선·기획안 근거를 확인했다.
독립 headless CLI에서도 파일 가져오기·개인 결과·모바일 390px·결과 그래프를 확인했다.
최종 앱 빌드·겨울 고정 코드 14개 확인이 통과했고, 기존 모델·DB 구조 변경은 없다.

## 게시 진행

**M6 앱 배포는 대기 상태다.** [CI 34302743602](https://github.com/gitvssh/fest-compass/actions/runs/34302743602)의 코드 검증과 이미지 빌드는 통과했으나, 저장량 985.1MiB + 새 레이어 86.7MiB가 1GiB 한도를 넘어 업로드가 실패했다. 운영 서비스는 기존 M5 이미지를 유지한다.

[정리 대상 네 개·보존본·승인 후 복구 절차](../ops/planning-outcomes-release.md)를 구체화했다. 새 대상 삭제·quota·권한 변경은 실행하지 않았다.
공개 M6 시험은 배포 전이므로 미실시다. 내부 Traceboard에 로컬 실제 화면과 검수 자료를 게시한다.
